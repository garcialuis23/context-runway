## What does this change do?

<!-- Short description of the change and why it's needed. -->

## How was this tested?

<!-- e.g. `npm test` in statusline/, manually loaded the extension in VS Code, etc. -->

## Checklist

- [ ] I ran `npm test` in `statusline/` and it passes
- [ ] I did not commit `node_modules/`, `.vsix` files, or personal data
- [ ] I did not add any new runtime dependencies without discussing them first
